version: '3.1'
services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - '5672:5672'
      - '15672:15672'
    environment:
      - RABBITMQ_DEFAULT_USER=eyyub
      - RABBITMQ_DEFAULT_PASS=eyyub

